// Placeholder until Task 20 (CLI bootstrap) replaces this file with the real
// stdio MCP server entrypoint. The empty export keeps TypeScript treating the
// file as a module under `verbatimModuleSyntax`.
// oxlint-disable-next-line unicorn/require-module-specifiers
export {};
